"use client";

import { ToolPageShell } from "@/components/tool-page-shell";

export default function UnirPage() {
  return (
    <ToolPageShell
      title="Unir PDF"
      description="Combina los archivos cargados en un solo documento, en el orden en que aparecen."
    />
  );
}
